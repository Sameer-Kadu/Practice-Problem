# Practice-Problem
Collection of LeetCode questions to ace the coding interview! - Created using [LeetHub v2](https://github.com/arunbhardwaj/LeetHub-2.0)
# Notes:
* Integer.toString(int) --> Convert integer to string
* Character.isLowerCase(ch) --> Check character present in string is lower case
* Character.isUpperCase(ch) --> Check character present in string is upper case
* s2.indexOf(s1) --> If s1 is substring of s2 the return the index of first occurance
* s2.lastIndexOf(s1) --> If s1 is substring of s2 return the indeex of last occurance
* Character.toLowerCase(ch) --> Convert upper case character to lower case character
* Character.toUpperCase(ch) --> Convert lower case character to upper case character
* string.charAt(i) --> Access each character of string
